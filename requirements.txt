# Thư viện cho bộ cào + đẩy sang EPG. Đều thuần Python, không cần trình biên dịch.
#   requests        gọi HTTP: cào trang, đẩy file lên EPG
#   beautifulsoup4  đọc HTML lịch phát sóng
#   xlwt            ghi file .xls đời 97-2003 (đúng định dạng EPG cần)
#   xlrd            đọc lại .xls (script sắp xếp)
requests==2.34.2
beautifulsoup4==4.15.0
xlwt==1.3.0
xlrd==2.0.2
